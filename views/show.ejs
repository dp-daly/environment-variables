<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= book.title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Caslon+Text:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
</head>
</head>
<body>
    <%- include('./partials/nav') %>
    <% if (typeof message !== 'undefined') { %>
        <p style="background-color:rgb(71, 255, 129);"><%= message %></p>
        <% } %>
    <div class="whole-book-record">
    <div class="book-body">
    <h1><i><%= book.title %></i>, <%= book.author %></h1>
    <%# I'd like to add conditional logic to NOT show image placeholder if no value was entered into the form or no src is present %>
    <img src="<%=book.image%>" alt="<%=book.title%> cover image" width="200">
    <h3><%= book.publisher %>, <%= book.year %></h3>
    <p><%= book.details %></p>
    <div class="reviews">
        <h1>Reviews</h1>
        <% book.reviews.forEach(review => { %>
            <%= review.text %>
            <% }) %>
    </div>
    <div class="action-wrapper">
        <button class="book-btn"><a href="/books/<%= book._id %>/edit">Edit details</a></button>
        <form action="/books/<%=book._id%>?_method=DELETE" method="POST">
          <button class="book-btn" type="submit">Remove</button>
        </form>
    </div>
    </div>

    <div class="column">
        <a href="/books/<%= book._id %>/reviews">Add a review</a>
        <a href="/books">Back to your shelf</a>
    </div>

</div>
</body>
<%- include('./partials/footer') %>
</html>