<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>This month's offering</title>
</head>
<body>
    <%- include('./partials/nav') %>
    <ul>
        <% books.forEach(book => { %>
        <li>
            <a href="/books/<%= book._id %>"><i><%= book.title %></i>, <%= book.author %></a>
        </li>
    <% }) %>
    </ul>
</body>
</html>